//                  Method
// Trim method is a method that eliminate the empty spaces before the string and after the strin
let msg=("   love you mom  ");
console.log(msg);
newMsg=msg.trim();
console.log(newMsg);
console.log(msg);
console.log(msg.trim());
let password=prompt("Set your password:");
console.log(password.trim());
// String is immutable in JavaScript
// toLowerCase(); method and toUpperCase();

let name=("muntazir malik");
console.log(name);
console.log(name.toUpperCase());
newName=name.toLowerCase();
console.log(newName);

//String methods with argument
//Argument is a value method that we pass to the method 
let str = ("i love coding");
console.log(str.indexOf("love"));
console.log(str.indexOf("l"));
console.log(str.indexOf("x"));

